# 更新 day8-plan.md - 标记不需要的表

## 任务概述

根据后端代码分析，确认 `day8-plan.md` 任务 2 中的两个表不需要创建：
1. 库存预警表 `inventory_warning` - 不需要（预警是实时计算的）
2. 库存调整记录表 `inventory_adjust_record` - 不需要（调整记录没有被持久化）

## 执行步骤

更新 `day8-plan.md` 文件，将这两个子任务标记为不需要：
- 将"添加库存预警表 `inventory_warning`"标记为 [N/A]
- 将"添加库存调整记录表 `inventory_adjust_record`"标记为 [N/A]

## 预期交付物

更新后的 `day8-plan.md` 文件